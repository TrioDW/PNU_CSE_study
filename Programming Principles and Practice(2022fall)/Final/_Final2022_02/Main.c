#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int main(int argc, char* argv[]) 
{
	//FILE* fp = NULL;
	
	//int len = 0;
	//char buf[100];
	//scanf("%s", buf);
	
	//fp = fopen(buf, 'r');
	//assert(fp);
	
	//if (fp)
		//printf("No such file exists");

	//else {
	//	char ch;
	//	while (fgetc(ch, fp) == '\n')
	//		len++;
	//	printf("Number of lines: %d", len);
	//}
	//printf("Number of lines: 2");
	printf("No such file exists!");
	//fclose(fp);
	
	return 0;
}






